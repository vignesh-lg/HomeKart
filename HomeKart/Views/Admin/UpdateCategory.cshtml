@model HomeKartEntity.ProductCategory
@{
    ViewBag.Title = "UpdateCategory";
    Layout = "~/Views/Shared/_UserLayout.cshtml";
}
@using (Html.BeginForm())
{
    @Html.AntiForgeryToken()
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <script src="https://kit.fontawesome.com/64d58efce2.js" crossorigin="anonymous"></script>
    <link href="~/Content/style.css" rel="stylesheet" />
    @*if (TempData["Message"] != null)
        {
            <p>
                @TempData["Message"].ToString()
            </p>
        }*@
    @Html.ValidationSummary(true, "", new { @class = "text-danger" })

    <div class="container">
        <div class="forms-container">
            <div class="signin-signup">
                <div class="sign-in-form" , id="myforms">
                    @Html.ValidationMessageFor(model => model.CategoryName, "", new { @class = "validation_message" })
                    @Html.ValidationMessageFor(model => model.Description, "", new { @class = "validation_message" })
                    <h2 class="title">Update Category</h2>

                    <div class="input-field">
                        <i class="fa fa-envelope"></i>
                        @Html.EditorFor(model => model.CategoryName, new { htmlAttributes = new { @class = "login-text", @placeholder = "Enter CategoryName" } })
                    </div>
                    <div class="input-field">
                        <i class="fas fa-envelope"></i>
                        @Html.EditorFor(model => model.Description, new { htmlAttributes = new { @class = "login-text", @placeholder = "Enter Description" } })
                    </div>
                    @Html.HiddenFor(model => model.CategoryId)
                    <input type="submit" value="Update" class="btn solid" />
                </div>
            </div>
        </div>
        <div class="panels-container">
            <div class="panel left-panel">
                <div class="content">
                    <h3>HOME KART</h3>
                    <p>
                        To go back please click below button
                    </p>
                    <a href='@Url.Action("ProductCategoryData", "Admin")' class="btn transparent" id="sign-up-btn">
                        Back
                    </a>
                </div>
            </div>
        </div>
    </div>
}

